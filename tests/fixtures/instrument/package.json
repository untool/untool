{
  "name": "untest",
  "mixin:core": "core",
  "mixin:server": "server",
  "mixin:browser": "browser",
  "untool": {
    "locations": ["/", "/foo"],
    "mixins": ["."]
  },
  "dependencies": {
    "@untool/defaults": "*"
  }
}
