{
  "name": "untest",
  "mixin:core": "core",
  "mixin:server": "server",
  "mixin:browser": "browser",
  "untool": {
    "browsers": "last 1 Chrome versions",
    "presets": ["@untool/defaults"],
    "mixins": ["."]
  }
}
